name: 001-TEST-VERSION

on: 
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. 设置 Java 17 (Gradle 运行必需)
      - name: set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 2. 设置 Flutter 环境
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # 3. 🛠️ 执行全套“代码手术”
      - name: Run Super Patch
        run: |
          # 修正 NDK 版本冲突
          sed -i '/android {/a \    ndkVersion = "28.2.13676358"' android/app/build.gradle
          
          # 修正过时的配置项
          sed -i '/synthetic-package/d' l10n.yaml
          
          # 修正代码中的 API 变更 (CardTheme -> CardThemeData)
          sed -i 's/CardTheme(/CardThemeData(/g' lib/theme.dart
          
          # 修正翻译文件中的类型冲突 (int/String -> Object)
          find . -name "*.arb" -exec sed -i 's/"type": "[^"]*"/"type": "Object"/g' {} +
          
          echo "✅ 所有代码手术已完成"

      # 4. 🛠️ 解决零件版本冲突 (解决本次报错的关键)
      - name: Fix intl Version
        run: |
          flutter pub add intl:^0.20.2
          flutter pub upgrade

      # 5. 获取依赖并打包
      - name: Build APK
        run: |
          flutter pub get
          flutter build apk --debug

      # 6. 上传 APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: My-BeeCount-APK
          path: build/app/outputs/flutter-apk/*.apk
