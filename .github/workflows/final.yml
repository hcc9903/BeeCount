name: 001-TEST-VERSION

on: 
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. 设置 Java 17 (老版本 Flutter 也需要它)
      - name: set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 2. 🛠️ 核心操作：强制降低 Flutter 引擎到 3.10.0
      # 这个版本依然支持 --no-sound-null-safety
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.10.0'
          channel: 'stable'

      # 3. 🛠️ 执行代码手术 (保留之前的全部补丁)
      - name: Run Intensive Surgery
        run: |
          sed -i '/android {/a \    ndkVersion = "28.2.13676358"' android/app/build.gradle
          sed -i '/synthetic-package/d' l10n.yaml || true
          find . -name "*.dart" -exec sed -i 's/CardTheme(/CardThemeData(/g' {} +
          find . -name "*.arb" -exec sed -i 's/"type": "[^"]*"/"type": "Object"/g' {} +
          echo "✅ 针对旧引擎的兼容补丁已完成"

      # 4. 解决零件冲突
      - name: Fix Dependencies
        run: |
          flutter pub add intl:^0.19.0 # 针对 3.10 版本，intl 0.19.0 更合适
          flutter pub upgrade

      # 5. 正式打包 APK (现在这个参数有效了)
      - name: Build APK
        run: |
          flutter pub get
          flutter build apk --debug --no-sound-null-safety

      # 6. 上传 APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: My-Legacy-BeeCount-APK
          path: "**/*.apk"
