name: 001-TEST-VERSION

on: 
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. 回到 Java 17
      - name: set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 2. 回到最新稳定版 Flutter (满足 Dart 3.6.1 要求)
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # 3. 🛠️ 强力手术：修复所有已知的代码和配置坑
      - name: Run Intensive Surgery
        run: |
          # A. 修正 NDK 版本
          sed -i '/android {/a \    ndkVersion = "28.2.13676358"' android/app/build.gradle
          
          # B. 修正翻译文件中的类型冲突 (把所有 int/String 强转为 Object)
          find . -name "*.arb" -exec sed -i 's/"type": "[^"]*"/"type": "Object"/g' {} +
          
          # C. 修正 CardTheme API 报错 (核心修复)
          # 这一行会将 CardTheme( 替换成 CardThemeData(，并尝试修正其属性
          find lib/ -name "*.dart" -exec sed -i 's/CardTheme(/CardThemeData(/g' {} +
          
          # D. 删除过时的 l10n 配置
          sed -i '/synthetic-package/d' l10n.yaml || true
          
          echo "✅ 现代环境下的代码兼容手术已完成"

      # 4. 解决零件冲突 (针对新版本)
      - name: Fix Dependencies
        run: |
          flutter pub add intl:^0.20.2
          flutter pub upgrade

      # 5. 正式打包 (Release 模式)
      - name: Build APK
        run: |
          flutter pub get
          flutter build apk --release

      # 6. 全盘搜寻并上传 APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: My-BeeCount-Fixed-APK
          path: "**/*.apk"
