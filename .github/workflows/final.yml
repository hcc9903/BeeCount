name: 001-TEST-VERSION

on: 
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. 设置 Java 环境 (Gradle 运行必需)
      - name: set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 2. 设置 Flutter 环境
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # 3. 🛠️ 执行“代码大手术” (针对报错精准修复)
      - name: Run Intensive Surgery
        run: |
          # 修正 NDK 版本冲突 (按照报错提示精准插入)
          sed -i '/android {/a \    ndkVersion = "28.2.13676358"' android/app/build.gradle
          
          # 修正 CardTheme 报错 (这是导致刚才失败的主因)
          # 把旧的 CardTheme( 替换成符合新标准的 CardThemeData(
          find . -name "*.dart" -exec sed -i 's/cardTheme: CardTheme(/cardTheme: CardThemeData(/g' {} +
          find . -name "*.dart" -exec sed -i 's/CardTheme(/CardThemeData(/g' {} +
          
          # 修正翻译文件中的类型冲突 (int/String -> Object)
          find . -name "*.arb" -exec sed -i 's/"type": "[^"]*"/"type": "Object"/g' {} +
          
          # 删除过时的配置项
          sed -i '/synthetic-package/d' l10n.yaml || true
          
          echo "✅ 所有的代码手术已经完成，老项目现在适配新环境了"

      # 4. 获取零件并强制升级 (解决 intl 冲突)
      - name: Fix Dependencies
        run: |
          flutter pub add intl:^0.20.2
          flutter pub upgrade

      # 5. 正式打包 APK
      - name: Build APK
        run: |
          flutter pub get
          # 加上 --no-sound-null-safety 以兼容可能存在的旧库
          flutter build apk --debug --no-sound-null-safety

      # 6. 全盘搜寻并上传 APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: My-Finished-BeeCount-APK
          path: "**/*.apk"
